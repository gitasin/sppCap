_schema-version: "3.1"
ID: sppCap
description: Generated by cds-services-archetype
version: 1.0.0
modules:
- name: sppCap_srv_dev
  type: java
  path: srv
  requires:
  - name: sppDb_hdi_dev
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    disk-quota: 1024M
    memory: 1024M
  build-parameters:
    build-result: target/*.[wj]ar
    builder: custom
    commands:
    - mvn clean package
- name: sppCap_db_dev
  type: hdb
  path: db
  requires:
  - name: sppDb_hdi_dev
  parameters:
    app-name: sppCap_db_dev
- name: sppCap-destination-content
  type: com.sap.application.content
  requires:
  - name: uaa_sppCap
    parameters:
      service-key:
        name: uaa_sppCap-key
  - name: sppCap_html_repo_host
    parameters:
      service-key:
        name: sppCap_html_repo_host-key
  - name: sppCap-destination-service
    parameters:
      content-target: true
  parameters:
    content:
      subaccount:
        destinations:
        - Name: sppCap_ui_dev_sppCap_html_repo_host
          ServiceInstanceName: sppCap-html5-app-host-service
          ServiceKeyName: sppCap_html_repo_host-key
          sap.cloud.service: sppCap_ui_dev
        - Authentication: OAuth2UserTokenExchange
          Name: sppCap_ui_dev_uaa_sppCap
          ServiceInstanceName: sppCap-xsuaa-service
          ServiceKeyName: uaa_sppCap-key
          sap.cloud.service: sppCap_ui_dev
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: sppCap_ui_deployer
  type: com.sap.application.content
  path: app
  requires:
  - name: sppCap_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - sampleMstMgr-content.zip
      name: sampleMstMgr
      target-path: resources/
    - artifacts:
      - sampleMgr-content.zip
      name: sampleMgr
      target-path: resources/
    - artifacts:
      - sampleGrpMgr-content.zip
      name: sampleGrpMgr
      target-path: resources/
    - artifacts:
      - controlOptionMgr-content.zip
      name: controlOptionMgr
      target-path: resources/
    - artifacts:
      - codeMgr-content.zip
      name: codeMgr
      target-path: resources/
    - artifacts:
      - currencyMgr-content.zip
      name: currencyMgr
      target-path: resources/
    - artifacts:
      - timeZoneMgr-content.zip
      name: timeZoneMgr
      target-path: resources/
- name: sampleMstMgr
  type: html5
  path: app/xx/sampleMstMgr
  build-parameters:
    builder: custom
    commands:
    - npm run build
    supported-platforms: []
- name: sampleMgr
  type: html5
  path: app/xx/sampleMgr
  build-parameters:
    builder: custom
    commands:
    - npm run build
    supported-platforms: []
- name: sampleGrpMgr
  type: html5
  path: app/xx/sampleGrpMgr
  build-parameters:
    builder: custom
    commands:
    - npm run build
    supported-platforms: []
- name: controlOptionMgr
  type: html5
  path: app/cm/controlOptionMgr
  build-parameters:
    builder: custom
    commands:
    - npm run build
    supported-platforms: []
- name: codeMgr
  type: html5
  path: app/cm/codeMgr
  build-parameters:
    builder: custom
    commands:
    - npm run build
    supported-platforms: []
- name: currencyMgr
  type: html5
  path: app/cm/currencyMgr
  build-parameters:
    builder: custom
    commands:
    - npm run build
    supported-platforms: []
- name: timeZoneMgr
  type: html5
  path: app/cm/timeZoneMgr
  build-parameters:
    builder: custom
    commands:
    - npm run build
    supported-platforms: []
resources:
- name: sppDb_hdi_dev
  type: com.sap.xs.hdi-container
  parameters:
    config:
      makeUniqueName: false
    service: hana
    service-plan: hdi-shared
  properties:
    hdi-service-name: ${service-name}
- name: sppCap-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      init_data:
        subaccount:
          destinations:
          - Authentication: NoAuthentication
            HTML5.ForwardAuthToken: true
            Name: srv-api
            ProxyType: Internet
            Type: HTTP
            URL: ~{srv-api/srv-url}
          existing_destinations_policy: update
    service: destination
    service-name: sppCap-destination-service
    service-plan: lite
  requires:
  - name: srv-api
- name: sppCap_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: sppCap-html5-app-host-service
    service-plan: app-host
- name: uaa_sppCap
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: sppCap-xsuaa-service
    service-plan: application
parameters:
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm install
