{
    "welcomeFile": "launchpad.html",
    "routes": [
        {
            "csrfProtection": false,
            "source": "^.*/srv-api/(.*)$",
            "destination": "srv-api",
            "target": "$1",
            "authenticationType": "none"
        },
        {
            "source": "^/(.*)",
            "localDir": "./",
            "authenticationType": "none"
        }
    ]
}